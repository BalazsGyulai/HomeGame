import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.szamolosjatek.com',
  appName: 'szamolos-jatek',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
